# Required for OpenRouter API access
OPENROUTER_API_KEY=sk-or-v1-your-api-key-here

# Optional: Set your deployment URL after first deploy
SITE_URL=https://your-app.vercel.app

# Optional: Custom site name for OpenRouter
SITE_NAME=RockyBot

# Optional: Override default model (defaults to openai/gpt-4o-mini)
OPENROUTER_MODEL=openai/gpt-4o-mini

# Optional: Force ephemeral mode (no file persistence)
# PERSIST_STORE=false
